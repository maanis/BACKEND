<!DOCTYPE html>
<html lang="en">

<head>
  <script src="https://kit.fontawesome.com/b4db02e187.js" crossorigin="anonymous"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<div class="container">
  <div class="todo-app">
    <h1><i class="fa-regular fa-pen-to-square"></i>To-do List:</h1>
    <form class="row" action="/createTodo" method="post">
      <input value='<% if (value.length > 0) { %><%= value %><% } %>' class="py-3 px-4" type="text" name="task"
        id="input-box" placeholder="Add a new task">
      <button class="bg-[#7a1cac] px-3 py-3 rounded-md text-white rounded-r-full" type="submit">Add</button>
    </form>




    <% if (data.length> 0) { %>
      <% data.forEach((e, i)=> { %>
        <ul id="list-container">
          <li class="flex shadow-lg">
            <p>
              <%= e, %>
            </p>
            <a class="ml-auto" href="/edit/<%= i %>">
              <button class="ml-auto mr-7 bg-[#7a1cac] px-3 py-1 rounded-md text-white">Edit</button>
            </a>
            <a href="/del/<%= i  %>">
              <button class="bg-[#7a1cac] px-3 py-1 rounded-md text-white">Del</button>
            </a>
          </li>
        </ul>
        <% }) %>
          <% } else { %>
            <h2>Add the task First</h2>
            <% } %>
  </div>

</html>