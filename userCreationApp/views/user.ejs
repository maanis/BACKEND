<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users Page</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-900 text-white">
    <div class="container mx-auto py-10">
        <h1 class="text-3xl font-bold text-center mb-10">User List</h1>
        <a href="/" class="text-blue-500 flex justify-center mb-5">back to create user</a>
        <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">


            <!-- Sample User Card -->
            <% if (data.length> 0) { %>
                <% data.forEach((e)=> { %>
                    <div class="bg-gray-800 rounded-lg shadow-lg p-5">
                        <img src="<%= e.imgurl %>" alt="User Image" class="w-24 h-24 rounded-full mx-auto mb-4">
                        <h3 class="text-xl font-semibold text-center mb-1">
                            <%= e.username %>
                        </h3>
                        <p class="text-gray-400  text-center mb-4">
                            <%= e.email %>
                        </p>

                        <div class="flex justify-center space-x-3">
                            <a href="/edit/<%=e._id %>">
                                <button
                                    class="px-4 py-2 bg-blue-600 hover:bg-blue-700 rounded-lg text-white font-semibold transition-colors duration-200">
                                    Edit
                                </button>
                            </a>
                            <a href="/del/<%=e._id %>"><button
                                    class="px-4 py-2 bg-red-600 hover:bg-red-700 rounded-lg text-white font-semibold transition-colors duration-200">
                                    Delete
                                </button></a>
                        </div>
                    </div>
                    <% }) %>
                        <% }else{ %>
                            <h2 class=" text-center absolute left-0 w-full">No Profiles
                                Yet</h2>
                            <% } %>

                                <!-- Additional user cards will go here, ideally generated dynamically -->

        </div>
    </div>
</body>

</html>