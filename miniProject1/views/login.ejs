<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram Clone Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-zinc-900 flex items-center justify-center min-h-screen">

    <div class="w-full max-w-sm bg-zinc-800 rounded-lg shadow-lg p-8">
        <!-- Instagram Logo -->
        <div class="flex justify-center mb-6">
            <img src="https://upload.wikimedia.org/wikipedia/commons/a/a5/Instagram_icon.png" alt="Instagram Logo"
                class="h-12 w-12">
        </div>

        <h2 class="text-3xl font-semibold text-white text-center mb-6">Log in to Instagram</h2>

        <!-- Form -->
        <form action="#" method="POST">
            <div class="mb-4">
                <input type="text" id="username-or-email" name="username-or-email"
                    class="w-full px-4 py-2 rounded bg-zinc-700 text-zinc-200 focus:outline-none focus:ring-2 focus:ring-blue-500"
                    placeholder="Username or email" required>
            </div>

            <div class="mb-4">
                <input type="password" id="password" name="password"
                    class="w-full px-4 py-2 rounded bg-zinc-700 text-zinc-200 focus:outline-none focus:ring-2 focus:ring-blue-500"
                    placeholder="Password" required>
            </div>

            <button type="submit"
                class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded-lg transition duration-300">
                Log In
            </button>

            <p class="text-zinc-400 text-center text-sm mt-4">
                Forgot your password? <a href="#" class="text-blue-500 hover:underline">Reset it</a>.
            </p>
        </form>

        <!-- Divider -->
        <div class="flex items-center my-6">
            <hr class="flex-grow border-zinc-700">
            <span class="text-zinc-500 px-3">OR</span>
            <hr class="flex-grow border-zinc-700">
        </div>

        <!-- Login with Facebook -->
        <div class="text-center mb-6">
            <button
                class="w-full bg-blue-800 hover:bg-blue-900 text-white font-semibold py-2 rounded-lg transition duration-300">
                Log in with Facebook
            </button>
        </div>

        <!-- Sign Up Redirect -->
        <p class="text-zinc-400 text-center">
            Don't have an account? <a href="#" class="text-blue-500 hover:underline">Sign up</a>
        </p>
    </div>

</body>

</html>