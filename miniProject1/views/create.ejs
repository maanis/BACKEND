<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Post - Instagram Clone</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-zinc-900 text-zinc-200 flex items-center justify-center min-h-screen">

    <div class="w-full max-w-md bg-zinc-800 rounded-lg shadow-lg p-6">
        <h2 class="text-2xl font-semibold text-white mb-6 text-center">Create New Post</h2>

        <!-- Create Post Form -->
        <form action="/create" method="POST" enctype="multipart/form-data">
            <!-- Image Upload -->
            <div class="mb-4">
                <label class="block text-sm font-medium text-zinc-300 mb-1" for="post-image">Upload Image</label>
                <input type="file" id="post-image" name="post" class="w-full text-zinc-300 focus:outline-none" required>
            </div>

            <!-- Caption -->
            <div class="mb-4">
                <label for="caption" class="block text-sm font-medium text-zinc-300">Caption</label>
                <textarea id="caption" name="caption" rows="3"
                    class="w-full mt-1 px-4 py-2 rounded bg-zinc-700 text-zinc-200 focus:outline-none focus:ring-2 focus:ring-blue-500"
                    placeholder="Write a caption..." required></textarea>
            </div>

            <!-- Submit Button -->
            <button type="submit"
                class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded-lg transition duration-300">
                Create Post
            </button>
        </form>
    </div>

</body>

</html>